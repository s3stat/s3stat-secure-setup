<UserControl x:Class="S3stat.SecureSetup.Content.EndpointDetailPlaceholder"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
		<StackPanel Margin="50,0,50,50">
			<TextBlock Text="Endpoint Setup" Style="{StaticResource Heading2}" />
			<TextBlock TextWrapping="WrapWithOverflow">
					Click on an item in the list to set it up with S3stat.
					<LineBreak/>
					<LineBreak/>
					<LineBreak/>
					<LineBreak/>
			</TextBlock>
		</StackPanel>
	</Grid>
</UserControl>
